#include "main.h"
#include <string.h>

/**
 * _strlen - returns length of string
 *
 * @s: parameter
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int n;

	n = strlen(s);
	return (n);

}

#include "man.h"
#include <stdio.h>

/**
 * _abs - computes absolute value of integer
 *
 *@n: parameter holding the number
 *
 * Return: absolute value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		rturn (n);
	}
	else
	{
		return (-n);
	}
}
